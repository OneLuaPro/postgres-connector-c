# postgres-connector-c
A lightweight CMake-based wrapper for the PostgreSQL libpq client library. It provides a minimal build environment—similar to `mariadb-connector-c` allowing you to compile libpq independently for client applications without the overhead of the full PostgreSQL suite.
